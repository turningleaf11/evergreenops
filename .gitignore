# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables / secrets
.env
.env.*
!.env.example
.npm-cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated locally when running the edge-function tests with `deno test`.
# Not committed: the project has never carried a Deno lockfile, and adding one
# would pin dependency resolution for every function as a side effect of local
# tooling rather than as a decision anyone made.
deno.lock
